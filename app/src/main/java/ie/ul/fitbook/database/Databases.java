package ie.ul.fitbook.database;

/**
 * This enum represents the possible Databases (i.e. collections) that we can access
 */
public enum Databases {
    /**
     * Represents the /users collection
     */
    USERS
}
